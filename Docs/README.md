# DocForge documentation

| Page | What's in it |
|---|---|
| [`ARCHITECTURE.md`](ARCHITECTURE.md) | How the pieces fit together — the pipeline, what each module is responsible for, the design principles used throughout. |
| [`CLI.md`](CLI.md) | Full `docforge` command reference — every subcommand, every flag. |
| [`MCP_SERVER.md`](MCP_SERVER.md) | Connecting an LLM client (Claude Code, Claude Desktop, LM Studio, your own agent) to `docforge-mcp`. |
| [`CONFIGURATION.md`](CONFIGURATION.md) | Every `.env`/environment variable, and the three ways to run the vector store. |
| [`ROADMAP.md`](ROADMAP.md) | What's been built, milestone by milestone, and what's next. |

For the top-level pitch and a quickstart, see the project [`README.md`](../README.md). For the
reasoning behind the architecture — not just what it is, but why it's built this way — see
[`GUID.md`](../GUID.md), the project's design decision log.
